string t = "abcde";  // time o(size!)
    sort(all(t));
    do
    {
      cout<<t<<endl;
    } while (next_permutation(all(t)));
    
---------------------------------------------------------------------

( 1LL << 50 ) long long long long
